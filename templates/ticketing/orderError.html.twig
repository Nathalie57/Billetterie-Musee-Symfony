{% extends 'base.html.twig'%}

{% block body %}
 
    <body class="is-preload">
        <div id="wrapper">
            <nav id="nav">
                <a href="{{ path('home') }}" class="icon solid fa-home"><span>Accueil</span></a>
                <a href="{{ path('opening_hours') }}" class="icon solid fa-clock"><span>Les horaires</span></a>
                <a href="{{ path('price') }}" class="icon solid fa-euro-sign"><span>Les tarifs</span></a>
                <a href="{{ path('choice') }}" class="icon solid fa-ticket-alt active"><span>Réservation</span></a>
		    </nav>

        <div id="main">
            <article id="billetterie" class="panel">
            
                {% if dateFalse %} 
                    <p>Vous ne pouvez pas acheter de billets pour le {{ visitDate | date('d/m/Y') }}.
                    Les dimanches, les jours fériés, les jours passés et le jour même après 14h ne sont pas ouverts à la réservation.</p><br/>
                    Le musée est fermé les mardi, le 1er mai, le 1er novembre et le 25 décembre.
                    
                {% else %}
                    {% if canBuyTicketsAmount > 0 %} 
                        Vous ne pouvez pas acheter {{ numberTickets }} billets pour le {{ visitDate | date('d/m/Y') }}.</br>
                        {% if canBuyTicketsAmount ==1 %} 
                            Il ne reste qu'un billet disponible à la réservation.
                            {% else %} Il ne reste que {{ canBuyTicketsAmount}} billets disponibles à la réservation.
                        {% endif %}
                    {% else %}Vous ne pouvez pas acheter de billets pour le {{ visitDate | date('d/m/Y') }}. Tous les billets ont déjà été vendus pour cette date.
                    {% endif %}
                {% endif %}
                <br/>
                <br/>

                <form action="{{ path('choice') }}">
                    <button type="submit">Retour à la billetterie</button>
                </form>

	        </article>
	    </div>
	<div id="footer">
		<ul class="copyright">
			<li>Mentions légales</li><li>&copy; Musée du Louvre - Tous droits de reproduction réservés</li><li><a href="http://www.louvre.fr">Retour sur le site du Musée</a></li><li>Tél : 01 40 20 50 50</li>
		</ul>
	</div>
	    </div>
    </body>

{% endblock %}

